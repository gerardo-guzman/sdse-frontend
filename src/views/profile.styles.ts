import { createStyles, Theme, makeStyles } from '@material-ui/core/styles';

export const profileClasses = makeStyles((theme: Theme) =>
createStyles({
    root: {
        flexGrow: 1,
        padding: '0.6rem'
    },
}),
);